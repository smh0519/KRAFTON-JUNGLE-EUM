version: '3.8'

services:
  livekit:
    image: livekit/livekit-server:latest
    container_name: eum-livekit
    command: --dev --bind 0.0.0.0 --node-ip 127.0.0.1
    network_mode: bridge
    ports:
      - "7880:7880"
      - "7881:7881"
      - "7882:7882/udp"
